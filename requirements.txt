# Phantasm 依赖
# AstrBot 运行环境一般已内置 httpx，这里显式声明便于独立测试。
httpx>=0.27.0
Pillow>=10.0.0

# 可选:使用 render.backend=html(Pillow 之外的 HTML→图片方案)时的额外依赖
#   pip install playwright && playwright install chromium
# 未安装时插件会自动回退到 Pillow 渲染(html 为默认 backend,回退不影响使用)。
# playwright
