# 更新日志

## v1.13.5 (2026-08-29)

- **翻译全过程留痕**（排查用，统一 `[翻译]` 前缀）：
  - 跳过 → `[x_1] [翻译] 跳过：中文字符占比 1.00 ≥ 阈值 0.30（正文 8 字）`
  - 需要 → `[x_1] [翻译] 需要翻译：平台标记 lang=ja（非中文） | 正文 13 字 → 调用模型`
  - 成功 → `[x_1] [翻译] 完成：13 字 → 2 字（模型：AstrBot 当前模型）`
  - 失败 → `[x_1] [翻译] 失败，回退原文：AstrBot 里没有可用的对话模型（…）`
  - 未启用 → DEBUG `[翻译] 未启用，跳过`
- 判断依据升级：**优先用平台语言标记**（X 的 `lang`，如 `ja`/`zh`），拿不到才退回「中文字符占比」
- 新增诊断命令 **`/phantasm tr <文本>`**：立刻用当前模型试翻译，一次看清「判断依据 / 用了哪个模型 / 译文或报错」

## v1.13.4 (2026-08-29)

- `credentials.x.rsshub_docker_socket` 现在同时支持 **HTTP 端点**：
  填 `http://dockerproxy:2375` / `tcp://…` 即走 TCP，填路径（如 `/var/run/docker.sock`）走 unix socket。
  这样可配 **tecnativa/docker-socket-proxy**（只开放 `CONTAINERS=1` + `POST=1`）实现一键生效，
  而不必把宿主机 Docker 的完全控制权交给 AstrBot 容器。
- 重建流程对「代理未开放 DELETE」更宽容：备份容器名加时间戳避免重名，删除备份改为**尽力而为**
  （失败只告警并给出清理命令，不影响重建成功）。
- 失败提示按端点类型区分（unix 提示挂载写法，TCP 提示检查代理与 CONTAINERS/POST 开关）；
  WebUI 字段与说明同步更新。

## v1.13.3 (2026-08-29)

- 修复：**翻译不生效**。AstrBot 里「配了模型但没设为默认」时 `get_using_provider()` 返回
  None，此前会直接放弃（静默回退原文）。现改为逐级兜底：
  配置指定 `provider_id` → 当前模型 → 异步取当前模型 → **任意一个启用中的对话模型**
  （会跳过 enable=false 的）。另加 `translate.provider_id` 可手动指定模型，
  首次成功解析会打 `[翻译] 使用模型：…`。
- `/phantasm status` 增加「翻译」一行（开关 / 目标语言 / 模型），便于一眼确认。
- 精简抓取失败提示（此前过长）：RSSHub / B 站 / X 的错误信息改为一句结论 + 必要的修复入口。

## v1.13.2 (2026-08-29)

- 诊断：RSSHub 返回 200 但 0 条推文时（auth_token 失效/被限流的典型表现）
  会打明确 WARN，且 `/视奸` 的「该账号暂无帖子」会附带**可操作提示**
  （查 `docker logs rsshub` 的 `is not valid`、在 WebUI 更新 Token）——此前该提示会误导排查方向。
- `credentials.x.rsshub_docker_socket`：Docker socket 路径可配置（默认 `/var/run/docker.sock`），
  失败提示带上实际路径；WebUI 新增对应输入框。

## v1.13.1 (2026-08-29)

- 修复：重建 RSSHub 容器的健壮性 —— 保留自定义网络别名（重建后同网络仍可用服务名互访）、
  不再复制 Hostname（避免指向已删容器）、HostConfig 不再传 Mounts（避免 duplicate mount point）、
  **启动失败也会回滚**旧容器。
- 修复：翻译场景渲出的原文卡片未按 `output_mode=temp` 清理，会一直堆积；现一并清理。

## v1.13.0 (2026-08-29)

- **设置页分页**（#5）：账号管理 / 渲染·翻译 / 投递 / 凭据·网络 / 状态 五个标签，记忆上次标签。
- WebUI 关于页显示真实插件版本（此前硬编码 1.0.0）。

## v1.12.0 (2026-08-29)

- **翻译**（#4）：用 AstrBot 已配置的 LLM 翻译外语正文，无需额外 API key。
  主卡片显示译文（带「译文」徽标），**原文卡片随附图一起放进合并转发消息**。
  可配目标语言 / 仅非中文时翻译 / 超长截断 / 自定义提示词。

## v1.11.0 (2026-08-29)

- **WebUI 配置 RSSHub Auth_Token**（#2）：填 token 后经 `docker.sock` 自动重建 RSSHub 容器使其生效；
  保留端口/挂载/网络/代理/重启策略，失败回滚。需挂载 `/var/run/docker.sock`。
- 修复：`_is_masked` 判据错误导致 WebUI 每次保存把**脱敏值当真实值写回**（会损坏 cookie / token）。

## v1.10.0 (2026-08-29)

- **X 引用帖独立子卡片**（#1）：拆出 RSSHub 的 `<div class="rsshub-quote">`，
  在主卡片内渲染引用子卡片（作者 / 正文 / 缩略图）；B 站转发同样接入。

## v1.9.0 (2026-08-29)

- **按账号过滤转推**（#3）：`/phantasm retweet <p> <id> on|off` + WebUI 开关；
  B 站转发动态、X 官方 API、X RSSHub 三路均标记并过滤。

## v1.8.7 (2026-08-29)

- X 时间固定转 **UTC+8**（卡片头部与文字说明一致）。

## v1.8.6 (2026-08-29)

- 修复 X 附图下载 404：RSSHub 的 `img src` / `video poster` 含 HTML 实体（`&amp;`），
  先 `html.unescape` 再下载。

## v1.8.4 (2026-08-29)

- X 转推识别：去掉正文 `RT <名字>:` 前缀，并显示「转推 @原作者」徽标。

## v1.8.0 ~ v1.8.3 (2026-08-29)

- 卡片与附图**拆成两条消息**：一条「说明(含链接)+卡片」，另一条用 `Nodes` **合并转发**
  打包原文卡片 + 附图（参考 pixiv_sender）。
- `/视奸` 改为与「检测到新帖」一致，并用 `event.send` 直接发送（修复 `chain_result` 无法发送转发链）。

## v1.7.0 ~ v1.7.2 (2026-08-29)

- **黑底主题**（仿 X 深色），Bilibili / X 分别开关；`render.theme_mode` + `render.dark_theme`。
- **账号代称**：`/phantasm alias`、WebUI 可编辑，`/视奸 <代称>` 按代称匹配。
- **`/视奸 <代称>`**：只输出该账号最新一条，带全局冷却（`send.cd_view_seconds`）。
- 修复：保存后黑白主题 / 代称不生效（`_reconfigure` 未同步 HTML 渲染器、WebUI 字段名不匹配）。

## v1.6.x (2026-08-29)

- 新增 **RSSHub 第三方 X 抓取**（`credentials.x.mode=rss`，不占官方额度），支持图片/视频封面/引用。
- 内网地址（`host.docker.internal`、`127.0.0.1`、内网 IP）**绕过代理**，修复自建 RSSHub 502。
- RSS 无互动数据时隐藏统计条；头像取自 `<channel><image>`；显示名取 `<author>`；正文保留换行。

## v1.5.0 (2026-08-25)

- 新增 **HTML（Playwright/Chromium）渲染后端**，彩色 emoji（Pillow 只能渲染白色剪影），
  不可用时自动回退 Pillow；WebUI 可切换渲染后端。

## v1.0.0 (2026-08-25)

- 首个版本。
- 支持监听 Bilibili（用户动态 REST API）与 X/Twitter（官方 API v2）。
- Pillow 卡片渲染：B 站 / X 平台主题色板、圆角、媒体网格自适应、统计条、平台水印、转发引用块。
- AstrBot 主动投递到配置的 QQ 群聊 / 私聊目标；每账号独立目标列表。
- 本地去重（processed.json，有界历史），同一帖不重复投递。
- `/phantasm` 系列命令：状态 / 检查 / 列表 / 增删账号与目标 / 暂停恢复。
- 插件设置页：凭据脱敏、账号与目标管理、状态面板。
- 代理、超时、重试、错误分类；单账号失败不影响整体。
