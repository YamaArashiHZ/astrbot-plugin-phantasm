# Phantasm project
__pycache__/
*.py[cod]
.temp/
.venv/
venv/
dist/
build/
*.egg-info/

# 运行时数据（每个 AstrBot 实例各自生成，不入库）
data/
cards/
*.log

# 凭证/密钥永远不应提交
# 示例：config 的 credentials 请只填 config.example.json 的占位符，或用环境变量
secrets.json
*.secret
.env
